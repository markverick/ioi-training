#include<stdio.h>
#include<algorithm>
int a[10000],b[10000];
int fA[10000];
using namespace std;
int main()
{
    int n,i,j,k;
    int a,b;
    int idx=0;
    int buffer;

    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d %d",&a[i],&b[i]);
        fA[]
    }
    sort(&a[0],&a[n])
}

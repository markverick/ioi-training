#include<bits/stdc++.h>

char str[2300];
bool pal[2300][2300];

bool dp(int x,int y)
{
    if(x==y)
    {
        return 1;
    }
    int i,j,k;
    for(i=x;i<=y;i++)
    {
        str[i]
    }

}
int main()
{
    int n;
    int i,j,k;
    scanf("%s",str);
    int len=strlen(str);
    for(i=0;i<len;i++)
    {
        pal[i][i]=1;
    }

}

#include<bits/stdc++.h>
using namespace std;
int dur=0;
int main()
{
    int n,i,j,k,a,b,c,d,x=1,y=1;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d %d %d %d",&a,&b,&c,&d);
        x*a/(max(1,min(10,x+b)));
    }
}
